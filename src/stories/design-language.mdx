import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Design Language/Overview" />

# The Matlock Raft Event design language

The rules behind the Matlock Raft Event site: what it's allowed to look like, and why.
If a choice isn't covered here, the principles decide. The logo is fixed and everything
else exists to frame it.

## Principles

1. **Village fête, not startup** — every element should feel at home on a riverside
   noticeboard on Boxing Day. If it would look right in a SaaS pitch deck, it's wrong here.
2. **The river runs through it** — water is the connective tissue: wave dividers, the river
   surface, the panorama. Sections don't just stack, they flow downstream.
3. **The logo is the mascot** — the badge supplies the whimsy. Typography, colour and layout
   stay confident and simple so the badge (and the photos) can be the characters.
4. **Say it once, clearly** — each fact lives in exactly one place per screen. Copy is
   truthful, warm and plain-spoken; invented claims are not on-brand.
5. **Things are physical** — buttons press, cards and countdown tiles tilt like pinned photos.
   Depth comes from hard offset shadows, never glows.
6. **Loud in one place** — one big moment per screen. Everything around it stays quiet so
   the loud thing lands.

## Typography

Two faces, no exceptions:

- **Anton** (display) — headlines, big numbers, card titles. Single weight; never pair with bold utilities.
- **Albert Sans** — everything else. Body is regular/medium sentence case at a 62–74ch
  measure; labels are ExtraBold caps with +0.12 to +0.2em tracking. That single label
  treatment covers buttons, nav links, eyebrows, chips, table headers and form labels —
  implemented as the `label-caps` (wide-tracked) and `label-caps-row` (tight, for fact rows
  and nav) utilities. Hard shadows come from the `shadow-card*` tokens, never literals.

## Motion

Interactions 130–250ms ease; reveals (the `Reveal` component) are a single 700ms
rise-and-fade on first view. Wave seams are **static by choice** — ambient motion is
limited to the marquee. Everything respects `prefers-reduced-motion`.

## Voice

Warm, concrete, a bit daft, always true. Labels in caps, sentences in sentence case,
one exclamation mark per page maximum.

## Decision log

| # | Decision | Outcome |
|---|----------|---------|
| 001 | Button & label face | Albert Sans ExtraBold caps (font-label) — strict two-font system; Rokkitt retired |
| 002 | Display face | Anton replaces Dela Gothic One; the logo is the only whimsical actor |
| 003 | Surfaces | Cut to cream / river / pine + pine-dark frame; sun demoted to ribbon accent; mint retired |
| 004 | Hero facts | Icon row + countdown caption; pill chips and prose subline rejected. Tappable fact cards (Red Bull style) and a CMS subtitle line were tried and rejected: the cards were off-style and repeated the countdown, the subtitle repeated the badge |
| 005 | Header badge | Hangs full-size at page top, shrinks to a small lip on scroll. Superseded by 010 |
| 006 | Status colours | Five families (success/warning/error/info/discovery), separate from brand accents |
| 007 | Wave seams | Reuse the existing four static wave SVGs; seam takes the incoming section&apos;s colour, adjacent seams vary the curve, no animation |
| 008 | Display case | Anton display type is always uppercase, sitewide |
| 009 | Heading alignment | Centred introduces a stage (card grids, podiums, CTAs); left-aligned with an eyebrow introduces reading (running prose). Grid under the heading &rarr; centre it; paragraphs &rarr; left-align it |
| 010 | Header | No badge in the bar: it's portrait and illegible at 56px. The bar carries the badge's own lettering (MATLOCK / RAFT / event, lifted from the badge as one colour) and the full badge appears only where it can be big: the home hero and the footer. Transparent over the home hero, solid pine once scrolled; a plain pine-dark rule, no wave edge or texture; nav in Anton; Donate is the standard raft button |
| 011 | Home hero | After Red Bull event pages: the photo fades into pine-dark and the badge, headline, fact row, buttons and countdown sit where the fade ends. Phones: photo band on top, content centred beneath. md and up: photo fills the hero, content left-aligned and vertically centred. The countdown is the hero's one playful element |
| 012 | Content width | Inside the section container, content caps at `max-w-5xl` (1024px) for grids and image-led sections and `max-w-4xl` (896px) for running text. The home hero follows the header's container instead, so it lines up with the lettering |
| 013 | Page header | Inner pages open on a printed, patterned ground (dots, straight tonal stripes or text wallpaper) with a huge Anton title in a hard shadow and the eyebrow on a stuck-on sticker or ribbon. One `color` prop sets the ground and everything on it; colour follows the page's family (river for Info, sun for joining in, pine-dark for sponsors and legal). Plain eyebrow on pages that shouldn't be playful (legal, updates, which can be tributes). Titles share one size and only step down past 20 characters. Illustration stickers (the recoloured raft, oar, water gun, wave and life ring) are on hold. Replaces the plain pine-dark masthead |
