import type { Shadows } from "@mui/material";

const transparent = "rgba(0, 0, 0, 0.25)";

const shadows: Shadows = [
  "none",
  `1px 1px 0px 0px ${transparent}`,
  `2px 2px 0px 0px ${transparent}`,
  `4px 4px 0px 0px ${transparent}`,
  `5px 5px 0px 0px ${transparent}`,
  `7px 7px 0px 0px ${transparent}`,
  `8px 8px 0px 0px ${transparent}`,
  `10px 10px 0px 0px ${transparent}`,
  `11px 11px 0px 0px ${transparent}`,
  `13px 13px 0px 0px ${transparent}`,
  `14px 14px 0px 0px ${transparent}`,
  `16px 16px 0px 0px ${transparent}`,
  `17px 17px 0px 0px ${transparent}`,
  `19px 19px 0px 0px ${transparent}`,
  `20px 20px 0px 0px ${transparent}`,
  `22px 22px 0px 0px ${transparent}`,
  `23px 23px 0px 0px ${transparent}`,
  `25px 25px 0px 0px ${transparent}`,
  `26px 26px 0px 0px ${transparent}`,
  `28px 28px 0px 0px ${transparent}`,
  `29px 29px 0px 0px ${transparent}`,
  `31px 31px 0px 0px ${transparent}`,
  `32px 32px 0px 0px ${transparent}`,
  `34px 34px 0px 0px ${transparent}`,
  `35px 35px 0px 0px ${transparent}`
];

export default shadows;
